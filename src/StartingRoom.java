public class StartingRoom extends Room{
    public StartingRoom(GameMap parent) {
        super(parent);
        desc = "There's a hatch in the ceiling, like the one you climbed in through.";
    }
}
